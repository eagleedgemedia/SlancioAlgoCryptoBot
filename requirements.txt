# ═══════════════════════════════════════════════════════
# Slancio Crypto Algo Treding Engine - Python Dependencies
# ═══════════════════════════════════════════════════════

# ── Core Trading Engine ──
pandas>=2.2.0
numpy>=1.26.0
requests>=2.31.0
aiosmtplib>=3.0.0           # Async SMTP for email OTP
aiohttp>=3.9.0               # Async HTTP for Fast2SMS SMS OTP

# ── API Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── Database ──
sqlalchemy>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0            # PostgreSQL async driver
aiosqlite>=0.20.0          # SQLite async driver (dev/testing)

# ── Security & Auth ──
python-jose[cryptography]>=3.3.0   # JWT tokens
bcrypt>=4.0.0                       # Password hashing (direct, avoids passlib bug)
cryptography>=42.0.0                # API key encryption (AES-256)
email-validator>=2.1.0              # Required by pydantic EmailStr

# ── Telegram ──
python-telegram-bot>=21.0

# ── Configuration ──
python-dotenv>=1.0.0
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0

# ── Logging & Monitoring ──
loguru>=0.7.0

# ── Scheduling ──
apscheduler>=3.10.4

# ── Testing ──
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0              # Async test client for FastAPI
